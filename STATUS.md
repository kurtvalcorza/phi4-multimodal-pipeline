# Release status

Current status: **Candidate / source-complete, awaiting clean-runtime execution**. The pipeline package (fleet snapshot scheme: `weights/phi4-multimodal-instruct/dimer-base-manifest.json` pinning the weights and the upstream remote code, `verify_snapshot`, `stage_missing_files`), offline unit tests, `MODEL_CARD.md`, and the standalone `MULTI-CAPABILITY` tutorial `tutorials/phi4_multimodal_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The 11 GB shards are not kept locally, so `verify_snapshot` over the real weights is unexercised until the clean GPU run. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
