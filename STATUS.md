# Release status

Current status: **Release-grade** — the `E2E` tutorial notebook `tutorials/phi4_multimodal_colab.ipynb` (blob `75ab9bb4`, committed at `1e348e2`) executed top-to-bottom in a clean Kaggle Tesla T4 runtime on 2026-09-20 (11/11 code cells ok (1 restart after the install cell), 2032.3 s, 391 files / 11,257 MB staged and digest-verified inside the notebook (the 26-entry snapshot including the five remote-code files, the 392 MB VizWiz parquet slice and the 336 pinned photographs)), recorded in `docs/release-verification.md`; adapted test CIDEr-D 0.890 against the frozen 4-bit model's 0.722 (sample-sanity, not a benchmark). Any later change to the carried modules or to the notebook yields a new blob and returns the registry to **Candidate** until a clean run of that blob is recorded. The notebook carries the pipeline package with its captioning adaptation contract (4-bit NF4 load, bounded fine-tuning of the checkpoint's own vision LoRA in the last eight decoder layers, safetensors adapter round trip), the digest-pinned VizWiz-Captions sample, the pinned snapshot manifest (weights **and** the five remote-code files) and the exact runtime pins. The promotion evidence is the clean run of the exact committed blob (no repository checkout, empty Hugging Face cache) recorded in `docs/release-verification.md`.

The **DIMER upload of the weights stays on HOLD** by Kurt's decision of 2026-09-20 (11.2 GB); the E2E carrier is built regardless. The multi-capability inference contract (text, image, audio) is preserved inside the E2E notebook.

What the repository ships: the package `phi4_multimodal_pipeline` (multimodal generation, `caption` / `evaluate` / `adapt` / artifact round trip, `metrics.py`, `samples.py`), 53 tests, the fleet notebook generator and its template, `tools/validate_release_assets.py`, and the documentation. The static checks (`ruff`, `pytest`, `python tools/validate_release_assets.py`, `python tools/build_notebook.py --check`) establish source conformance, not DIMER readiness.
